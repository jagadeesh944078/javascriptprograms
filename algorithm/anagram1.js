const utility=require('../utility/utility')
const readline=require('readline-sync')
var str1=readline.question('enter the value')
var str2=readline.question("enter the value")
 utility.anagram(str1,str2);
